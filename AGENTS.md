# AGENTS.md — 返済試算 PWA 開発ルール

## 1. プロジェクト概要

このリポジトリは、日本語の返済試算 PWA です。主な利用環境は Android の Chrome / ホーム画面に追加した PWA で、画面幅 360〜430px のスマートフォンを最優先にします。

- 現在の安定ベース: **Ver2.36.4**
- アプリ本体の構成: **静的 HTML + CSS + Vanilla JavaScript**
- ストア公開準備: **Capacitor による Android / iOS ラッパー**
- アプリ本体へフレームワーク、ビルドツール、外部 UI ライブラリは導入しない
- 公開方法: GitHub Pages + App Store / Google Play 準備
- PWA: `manifest.json` + `sw.js`

最初に必ず `PROJECT_STATUS.md` を読み、現在の実装状況と保留事項を把握してください。

## 2. 重要ファイル

| ファイル | 役割 |
|---|---|
| `index.html` | アプリ本体。HTML / CSS / JavaScript が 1ファイルに統合されている |
| `manifest.json` | PWA 名称、起動モード、テーマ色、アイコン設定 |
| `sw.js` | Service Worker。オフライン時の最低限のキャッシュ処理 |
| `package.json` / `capacitor.config.json` | Android / iOS ストア包装用の Capacitor 設定 |
| `android/` / `ios/` | Capacitor 生成のネイティブプロジェクト |
| `privacy.html` / `support.html` | ストア提出用の公開URL |
| `store/` | ストア公開前チェックリストと掲載文案 |
| `icon-*-v2362-circle.png` | 現行 Ver2.36.4 の PWA アイコン |
| `icon-180-v2362-circle.png` | iOS / Apple touch icon 用アイコン |
| `proposal_logo_light_transparent.png` | ホーム画面用ロゴ素材 |

### 現行の重要な実装箇所（`index.html`）

- `calc(data)`：返済計算。**勝手に計算式を変更しない**
- `japaneseHolidaySet()` / `nextBiz()`：休日・振替休日・営業日補正。**勝手に削除・簡略化しない**
- `pushHistory()`：localStorage に直近5件を保持する履歴処理
- `renderSingle()`：通常試算の結果画面
- `renderCompare()`：比較画面
- `tableHTML()`：返済予定表
- `initAmbientDust()`：ホーム背景の粒子
- `initSplash()`：起動画面の粒子アニメーション

## 3. 絶対に守ること

1. **関係ない UI や機能を巻き戻さない。**
   依頼された箇所だけを最小限に変更する。

2. **返済計算・休日補正・履歴保存を壊さない。**
   変更が必要な場合は、先に影響範囲・計算根拠・テスト方法を説明する。

3. **読み込まれない CSS / JavaScript ファイルだけを追加しない。**
   別ファイル化する場合は、必ず `index.html` から確実に読み込む。

4. **main へ直接変更しない。**
   作業はブランチと Pull Request 単位で行い、変更内容・確認方法・リスクを PR に明記する。

5. **モバイル表示を最優先にする。**
   360px、390px、430px の幅で確認し、横幅不足・文字切れ・過度な横スクロールを避ける。

6. **ダーク / ライトの両方を確認する。**
   背景、文字、入力欄、ボタン、粒子、表、PDF印刷が片方だけ崩れないようにする。

7. **PWA のキャッシュ更新を忘れない。**
   起動に影響する HTML・CSS・JS・画像・manifest を更新した場合は、必要に応じて `sw.js` の CACHE 名と ASSETS を更新する。

8. **データ互換性を維持する。**
   localStorage の既存キー・履歴・現在条件が読めなくなる変更は避ける。形式変更が必要なら移行処理を追加する。

9. **依頼のないデザインの全面刷新をしない。**
   現在の方向性は「深いネイビー、淡い青白い光、金融アプリとしての可読性、過度に銀行っぽくしない上品さ」。

10. **日本語表示を優先する。**
    説明文、エラー、ボタン、印刷物は自然な日本語にする。

## 4. UX / デザイン方針

### 基本

- 画面は「高級感のあるネイビー × 青白い光」の世界観
- 金融アプリとして、演出より数値の読みやすさを優先
- 重要な金額は大きく、補足情報は控えめに
- 画面の情報密度は高めでも、1つのカードに詰め込みすぎない
- 余計な専門用語は避け、比較時は数字の差がすぐ分かる設計にする

### ダークモード

- 背景の粒子は「星空」ではなく、魔法の粉が一方向に流れた余韻
- 粒は細かく、グローで視認性を出す
- 背景粒子は内容を邪魔しないこと

### ライトモード

- 白背景で粒子が消えないよう、淡い青だけでなく青の芯・薄い影を使う
- 眩しさ、低コントラスト、淡すぎる境界線に注意する

### 起動画面

現行演出は一時的な実装で、今後差し替え予定です。詳細は `PROJECT_STATUS.md` を参照してください。

## 5. 返済計算の注意

- 元利均等、元金均等、ボーナス加算に対応している
- 休日・振替休日・国民の休日は次営業日に補正する仕様
- 初回日割り利息を扱う
- 返済表、比較、PDF は同じ計算結果を使うこと
- 表示上の丸めと内部計算の丸めを安易に統一・変更しない

計算に関する変更を行う場合は、少なくとも次を手動確認してください。

- 元利均等（通常）
- 元利均等（ボーナス払いあり）
- 元金均等
- 初回日割りあり
- 返済日が土日祝日に当たるケース
- 最終回の元金・残高が不自然にならないこと

## 6. リリース前チェック

変更後は、少なくとも以下を確認してから PR を出してください。

### 機能

- 新規試算が完走する
- 編集・再試算ができる
- 比較モードが開く
- 履歴が5件まで残り、6件目で最古が消える
- PDF印刷で1ページ目が空白にならない
- 和暦 / 西暦表示がある場合は通常・比較・印刷で整合する

### 表示

- 360 / 390 / 430px 幅
- ダーク / ライト
- 画面回転なしの縦表示
- ホーム画面追加後の standalone 起動
- Chrome の通常タブ表示
- 印刷プレビュー（A4縦）

### PWA

- `manifest.json` のアイコン参照が存在する
- `sw.js` が更新済みの必要ファイルをキャッシュ対象にしている
- キャッシュ名が必要に応じて更新されている
- 古いキャッシュが残り続けない

## 7. 作業の進め方

1. 依頼内容を短く要約する
2. 影響する関数・HTML・CSSを特定する
3. 変更前に、壊れやすい箇所を明記する
4. 最小差分で実装する
5. 上記の確認を行う
6. PR には次を日本語で書く
   - 変更内容
   - 変更していない重要機能
   - 確認した項目
   - 残るリスク / 未確認項目
