# PROJECT_STATUS.md — 返済試算 PWA 現状・引き継ぎメモ

最終更新: 2026-06-25
現行の安定ベース: **Ver2.36.1**

## 1. 現在の状態

`index.html`、`manifest.json`、`sw.js` の現行実装は Ver2.36.1 です。

過去に Ver2.36.2 として「UIパッチ」が作られましたが、現行 `index.html` はその CSS / JavaScript を読み込む構成ではありません。該当する `v2362-ui.css` / `v2362-ui.js` は削除済みのため、**Ver2.36.2 は実装済み・反映済みとして扱わないでください。**

以後の作業は、必ず Ver2.36.1 の現行ソースを基準に行います。

## 2. 現在実装済みの機能

### アプリ共通

- ダークモード / ライトモード切替
- PWA 設定（`display: standalone`）
  - ホーム画面に追加したアプリとして起動すると、通常の Chrome URLバーなしで開く
- Service Worker による最低限のキャッシュ
- 右上メニュー
- 直近5件の履歴保存
  - localStorage を使用
  - 6件目以降は古い履歴を自動削除
- 保存済み条件（最大20件）
- PDF出力 / 印刷

### 試算入力

- ステップ形式
  1. 返済方式
  2. 借入金額
  3. 年利率
  4. 返済期間 / 回数・毎月元金
  5. 返済日・実行日・ボーナス払い
- 元利均等返済
- 元金均等返済
- ボーナス加算（元利均等のみ）
- 金額単位の切替（万円 / 円）

### 計算・返済日

- 初回日割り利息
- 返済日が休日、振替休日、国民の休日に当たる場合の次営業日補正
- 元金、利息、返済額、残高の返済予定表

### 結果画面 / 印刷

- 結果サマリー（毎月返済額、利息総額、支払総額、返済回数）
- PDF印刷用の濃紺カバー + 白い予定表デザイン
- 通常試算と比較試算の入口

### 背景・起動画面

- ホーム背景に Canvas ベースの粒子アニメーション
- 起動画面に Canvas ベースの粒子アニメーション
  - 現行は「下から粒子が湧く → 中央に集まる → 円運動 → 消える」演出
  - 終了後、**新規試算の入力ウィザードを自動で開く**

## 3. 現在の課題 / 未実装の優先タスク

以下の 1〜5 は、ユーザーと合意済みですが、**現行の Ver2.36.1 にはまだ統合されていません。**

### 優先 1 — 比較画面を「同じ項目の横比較」に作り直す

現状:

- A/B の小さいカードを並べるだけ
- 詳細表は「Aの予定表 → Bの予定表」と縦に続く
- 比較として視線移動が多い

目標:

- 上部は1画面で A / B / 差額を横比較
- 表示項目:
  - 条件（借入金額、年利率、期間 / 回数）
  - 毎月返済額
  - 総返済額
  - 利息総額
  - 返済回数
- 「B は A より月々 +○円、総額 +○円」を明確に表示
- A/B の入替、編集、比較終了を用意
- スマホで横スクロールを極力出さない

詳細表:

```text
回数 ｜ 返済日 ｜ A返済額 ｜ B返済額 ｜ 差額
```

を基本にし、必要なら `返済額 / 元金 / 利息 / 残高` の表示切替を置く。

### 優先 2 — 返済予定表を高密度化する

現状:

- スマホでは各返済回が大きなカードになる
- 120回などで縦に長く、一覧性が低い

目標:

- 通常・比較の両方を、コンパクトな表形式へ
- 基本列:

```text
回 ｜ 返済日 ｜ 返済額 ｜ 元金 ｜ 利息 ｜ 残高
```

- 横幅が足りない場合は、表の軽い横スクロールを許可してもよい
- ただしカードを1回ずつ縦に並べない
- 初回日割りは表の上に小さく表示し、1行目には小さな `初回` バッジ程度にする

### 優先 3 — 和暦 / 西暦の切替ボタン

現状:

- `jpDate(date, wareki)` の処理はある
- `wareki: true` が初期値
- ユーザーが切替できる UI はない

目標:

```text
[ 和暦 ] [ 西暦 ]
```

- 通常の返済予定表に配置
- 比較詳細表にも同じ表示設定を反映
- PDF出力にも同じ表示を反映
- 選択状態は localStorage に保存

### 優先 4 — ホームの返済サマリー帯に PDF の円弧モチーフを移植

現状:

- PDF印刷のカバーには円弧・リング・光点のモチーフがある
- ホームの結果サマリー帯には同じ世界観が十分反映されていない

目標:

- 結果サマリー帯の右側背景に薄く配置
- 右端から切れる二重円弧
- 小さい光点を1つ程度
- 数字や条件文を邪魔しない透明度
- ダーク / ライトで濃さを調整
- PDFとホームの世界観を揃える

### 優先 5 — ホーム背景粒子を両モードで少し見えやすくする

現状:

- 粒子は存在するが、ユーザーには存在感が弱く見える

目標:

- 粒を大きくしすぎず、細かい「魔法の粉」感を維持
- ダーク: 青白い芯と発光を少し強くする
- ライト: 青の芯と薄い影で白背景でも埋もれないようにする
- ただの星空ではなく、一方向に流れた粉の余韻を見せる
- 低負荷を維持する（粒子数を無制限に増やさない）

## 4. 次の優先タスクではないが、合意済みの方向性

### 起動画面の再設計（優先6、別バージョンで実施）

現行の起動画面を次へ差し替える予定。

```text
左から細かい魔法の粉がふわっと流れ込む
↓
中央で円形ローディングになる
↓
先頭が最も明るく、後ろが尾を引く
↓
2〜3周する
↓
ろうそくを吹き消すように右へ「フッ」と散る
↓
ホーム画面へ
```

- ロゴは起動画面で無理に完成表示しない
- バージョン表記は下部に控えめに置く
- その下に淡いカーブ状の線を置く
- 起動後は入力ウィザードを自動で開かず、ホームを表示する
- 全体の長さの目安は約4秒
- アニメーションと PNG ロゴの位置合わせは過去に不安定だったため、ロゴ完成演出は避ける

## 5. デザイン上の合意事項

- ユーザーは「見た目重視」だが、金融計算アプリなので数字の可読性を最優先する
- ダークは深いネイビー、青白い発光、控えめな魔法の粉の余韻
- ライトは青系の上品な明るさ。粒子が白背景で消えないこと
- ロゴはホーム画面・アイコンで使う。起動画面で無理にロゴ形状を描かない
- 比較画面は、一般の人でも「月々」「総額」「差額」が即座に分かること
- グラフは現時点では優先度が低い。数字の比較を優先する

## 6. リリース / バージョン運用

次の作業を開始する前に、現在の Ver2.36.1 を Git タグまたはリリースで固定することを推奨します。

例:

```text
タグ: v2.36.1-baseline
```

次の実装候補のバージョン:

```text
Ver2.36.2
```

バージョンを更新する際は、必要に応じて以下を揃える。

- `index.html` の `<title>`
- 起動画面のバージョン表記
- PDF印刷のバージョン表記
- `manifest.json` のアイコン参照（アイコン更新時）
- `sw.js` の CACHE 名
- 新しいアイコン・画像ファイルのキャッシュ対象

**localStorage のキーを変更する場合は、旧キーから読める移行処理を残すこと。**

## 7. Codex の最初のタスク

最初はコードを変更せず、現状監査だけを行う。

確認事項:

1. `index.html`、`manifest.json`、`sw.js`、画像素材の参照関係
2. 返済計算、休日補正、履歴保存、比較、PDF印刷、起動画面の実装箇所
3. 上記の優先1〜5を実装する場合の変更箇所とリスク
4. スマホ表示・ダーク / ライト・印刷で壊れやすい箇所
5. 最小差分で着手する順番

この監査では、ファイル変更・削除・PR作成をしないこと。

