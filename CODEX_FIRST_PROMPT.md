# Codex に最初に貼る依頼文

このリポジトリは、日本語の返済試算 PWA です。現在の安定ベースは Ver1.3.4 です。

まず `AGENTS.md` と `PROJECT_STATUS.md` を全文読んでください。

今回のタスクでは、コードを変更・削除・コミット・Pull Request 作成せず、**現状監査だけ**をしてください。

確認してほしいこと：

1. エントリーファイル、PWA設定、Service Worker、画像素材の参照関係
2. 返済計算、休日補正、履歴保存、比較モード、PDF印刷、起動画面の実装箇所
3. 現在実装済みの機能と、未実装またはUI改善が必要な機能
4. `PROJECT_STATUS.md` の優先1〜5を実装する場合の変更対象ファイル、関数、リスク
5. スマホ表示・ライト/ダークモード・印刷で壊れやすい箇所
6. 最初に着手するべき作業を、影響範囲が小さい順に提案

重要な制約：

- 静的HTML/CSS/JavaScriptの構成を維持する
- 既存の返済計算、休日補正、localStorageの履歴を壊さない
- index.htmlに読み込まれないCSS/JSファイルだけを追加しない
- 関係のないUIを巻き戻さない
- 修正案があっても、今回はコード変更をしない

最後に、日本語で次の3見出しだけを使って簡潔に報告してください。

- 現状理解
- リスク
- 推奨する最初の実装タスク
